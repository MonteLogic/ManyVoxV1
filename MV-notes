There saying that its getting cleaned up by other parts of the file. 

So I'm going to create a new file where I only write. 

Look at the file paths the first one shows wheres its actually at, the other ones are relative. 

So its a matter of hashing out the right parent files. 
